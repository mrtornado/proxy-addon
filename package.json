{
  "name": "proxy-addon",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build-extension": "yarn build && cp src/manifest.json dist/ && cp src/assets/* dist/assets/ && cp src/background.js dist/",
    "preview": "vite preview"
  },
  "dependencies": {
    "chrome-extension": "^0.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.28",
    "@types/react-dom": "^18.0.11",
    "@vitejs/plugin-react": "^3.1.0",
    "typescript": "^4.9.3",
    "unocss": "^0.50.6",
    "vite": "^4.2.0"
  }
}
